# Compose

Docker Compose service definitions for the VPS will live here.

This scaffold intentionally does not include real service manifests yet. Do not commit environment files, tokens, credentials, or generated runtime artifacts.
