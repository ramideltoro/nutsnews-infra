# Ansible

Ansible inventory, playbooks, roles, and host configuration automation will live here.

This scaffold intentionally does not include real Ansible automation yet. Do not commit host credentials, private keys, vault passwords, or generated local artifacts.
