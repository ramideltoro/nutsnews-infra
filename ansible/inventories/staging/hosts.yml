all:
  children:
    nutsnews_staging_vps:
      hosts:
        staging-vps:
          # Staging is an isolated runtime on the capacity-reserved VPS.  The
          # fixed deploy play can name only this inventory alias, never the
          # production inventory group or production app container.
          ansible_host: 65.75.202.112
          ansible_user: nutsnews_ops
