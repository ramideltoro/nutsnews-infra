# actionlint 1.7.12 predates GitHub Actions' official concurrency queue key.
# Keep this ignore exact and let validate_workflow_concurrency.py enforce the
# supported queue contract until actionlint ships parser support.
paths:
  .github/workflows/**/*.{yml,yaml}:
    ignore:
      - 'unexpected key "queue" for "concurrency" section'
